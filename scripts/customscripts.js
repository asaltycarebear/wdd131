function openGoogleFonts(event) {
  event.preventDefault(); // Prevent default link behavior
  window.open("https://fonts.google.com/", "Brose Fonts - Google Fonts"); // Reuses the tab if already open
}
